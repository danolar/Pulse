// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0 <0.9.0;

/// @notice Community adapters implement reporting through PulseOracle.reportSignal.
interface ISignalAdapter {
    /// @dev Marker interface — adapter logic lives offchain (CRE, backend, etc.).
}
